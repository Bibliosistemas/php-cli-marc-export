# php-cli-marc-export
This script uses File_MARC to read a .mrc file and export as a single file or individual files marc21 records in marcxml format.
Also clean some weird characters in the source.   
